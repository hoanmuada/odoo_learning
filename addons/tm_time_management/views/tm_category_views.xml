<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- ===== LIST VIEW ===== -->
    <record id="view_tm_category_tree" model="ir.ui.view">
        <field name="name">tm.category.tree</field>
        <field name="model">tm.category</field>
        <field name="arch" type="xml">
            <list string="Categories" editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="code"/>
                <field name="color" widget="color_picker"/>
            </list>
        </field>
    </record>

    <!-- ===== FORM VIEW ===== -->
    <record id="view_tm_category_form" model="ir.ui.view">
        <field name="name">tm.category.form</field>
        <field name="model">tm.category</field>
        <field name="arch" type="xml">
            <form string="Category">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="code"/>
                        </group>
                        <group>
                            <field name="sequence"/>
                            <field name="color" widget="color_picker"/>
                            <field name="active"/>
                        </group>
                    </group>
                    <group string="Description">
                        <field name="description" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ===== ACTION ===== -->
    <record id="action_tm_category" model="ir.actions.act_window">
        <field name="name">Categories</field>
        <field name="res_model">tm.category</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first time category!
            </p>
            <p>
                Categories help you classify your time entries
                (e.g. Development, Meeting, Training).
            </p>
        </field>
    </record>

</odoo>
